<section id="contacto" class="p-10 pt-25 scroll-m-25 background-fade">
    <div class="container mx-auto flex flex-col gap-y-10">
        <div class="flex justify-center">
            <h1 class="background-h1">Contáctanos</h1>
        </div>
        <div class="flex flex-col gap-y-5">
            <p>
                Déjenos su consulta, nos pondremos en contacto con usted en la
                mayor brevedad posible.
            </p>
            <p class="font-semibold">
                ¡No esperes más y hablemos de tu proyecto eléctrico!.
            </p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 bg-white p-7 lg:p-7 rounded-2xl">
            <!-- FORMULARIO -->
            <form class="space-y-6">
                <!-- Nombre + Email -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                    <div>
                        <label class="text-gray-600 text-sm">Tu nombre</label>
                        <input
                            type="text"
                            placeholder="Nombre"
                            class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue"
                        />
                    </div>

                    <div>
                        <label class="text-gray-600 text-sm">Tu email</label>
                        <input
                            type="email"
                            placeholder="Email"
                            class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue"
                        />
                    </div>
                </div>

                <!-- Asunto -->
                <div>
                    <label class="text-gray-600 text-sm">Asunto</label>
                    <input
                        type="text"
                        placeholder="Asunto"
                        class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue"
                    />
                </div>

                <!-- Teléfono -->
                <div>
                    <label class="text-gray-600 text-sm">Teléfono</label>
                    <input
                        type="text"
                        placeholder="Teléfono"
                        class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue"
                    />
                </div>

                <!-- Servicio -->
                <div>
                    <label class="text-gray-600 text-sm"
                        >Seleccione un servicio</label
                    >
                    <select
                        class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue"
                    >
                        <option value="">Seleccione…</option>
                        <option>Instalación eléctrica</option>
                        <option>Mantenimiento</option>
                        <option>Revisión</option>
                    </select>
                </div>

                <!-- Mensaje -->
                <div>
                    <label class="text-gray-600 text-sm"
                        >Danos más detalles</label
                    >
                    <textarea
                        rows="5"
                        placeholder="Mensaje"
                        class="w-full border border-gray-300 rounded-md p-3 focus:outline-accent-blue resize-none"
                    ></textarea>
                </div>

                <!-- Botón -->
                <button
                    type="submit"
                    class="bg-accent-blue hover:bg-primary-orange cursor-pointer text-white font-bold px-8 py-3 rounded-md transition duration-300"
                >
                    ENVIAR
                </button>
            </form>

            <!-- INFORMACIÓN LATERAL -->
            <div class="space-y-8">
                <!-- Dirección -->
                <div class="flex items-center gap-4">
                    <div class="p-3 border border-gray-300 rounded-full">
                        📍
                    </div>
                    <p class="text-gray-600">
                        C. Tungsteno, Norte, 41007 Sevilla
                    </p>
                </div>

                <!-- Email -->
                <div class="flex items-center gap-4">
                    <div class="p-3 border border-gray-300 rounded-full">
                        ✉️
                    </div>
                    <p class="text-gray-600">jesusgomezgranado@gmail.com</p>
                </div>

                <!-- Teléfono -->
                <div class="flex items-center gap-4">
                    <div class="p-3 border border-gray-300 rounded-full">
                        📞
                    </div>
                    <p class="text-gray-600">691 88 63 70</p>
                </div>

                <!-- Texto adicional -->
                <p class="text-gray-600 text-sm leading-relaxed font-semibold">
                    Le llamaremos y le daremos una estimación del coste de su proyecto en cuanto nos lo envíe.
                </p>

                <p class="text-gray-600 text-sm font-semibold">
                    Mantendremos sus datos 100% confidenciales
                </p>

                <!-- Logo -->
                <div class="pt-6">
                    <img
                        src="src/assets/logo_jesus_completo.png"
                        alt="Logo"
                        class="w-56 opacity-80 mx-auto lg:mx-0"
                    />
                </div>
            </div>
        </div>
    </div>
</section>
